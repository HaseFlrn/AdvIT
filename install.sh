#!/usr/bin/bash
snap install spotify && snap install discord && snap install code-classic && snap install flutter && flutter update && apt install vlc && apt install firefox && apt install thunderbird